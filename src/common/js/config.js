/**
 * @author lenovo
 * @date 2020/2/21 9:27
 * @desc 配置信息
 */

/**
 * 播放模式
 * @type {{sequence: number, random: number, loop: number}}
 */
export const playMode = {
  // 顺序播放
  sequence: 0,
  // 单曲循环
  loop: 1,
  // 随机播放
  random: 2
}
